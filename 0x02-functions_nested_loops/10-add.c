#include "main.h"
/**
 * add - adds two integres and returns the result
 * @a: int to added to b
 * @b: int to added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
int c;
c = a + b;
return (c);
}
